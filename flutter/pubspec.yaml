name: tracksmart_mobile
description: TrackSmart Mobile — Flutter port of the Next.js mobile app.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  go_router: ^14.2.0
  flutter_map: ^7.0.0
  latlong2: ^0.9.0
  intl: ^0.19.0
  signature: ^5.4.0
  url_launcher: ^6.3.0
  shared_preferences: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
